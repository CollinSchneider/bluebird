<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <h6><%= @user.first_name %>,</h6>

    <div class="center-align">
      <div class="row">
        <p>
          Looks like your purchase order of <%= @product.title %> didn't hit its sales goal.
        </p>
      </div>
      <div class="row">
        <h6>
          You now have 7 days to purchase <%= @product.title %> for the full wholesale price of $<%= @product.price %> by folling the link below.
        </h6>
        <h6>
          <a href="localhost:3000/full_price/<%= @product.product_token.token %>/<%= @product.slug %>">Purchase Here</a>
        </h6>
      </div>
      <div class="row">
        Your friends, <br>
        BlueBird.club
      </div>
    </div>


  </body>
</html>
