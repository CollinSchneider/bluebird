<%= render partial: 'partials/wholesaler_profile_nav' %>

<div class="col s12 m8">

<div class="center-align row col s8 offset-s2">
  <div class="row">
    <% if flash[:error] %>
      <% flash[:error].each do |err, msg| %>
        <h6 class="red-text"><%= msg %></h6>
      <% end %>
    <% end %>
  </div>
  <div class="row">
    <h6 class="green-text"><%= flash[:success] %></h6>
  </div>
  <%= form_for current_user, url: "/settings/wholesaler/change_password", method: 'PUT' do |f| %>
    <div class="input-field">
      <%= f.label "New Password" %>
      <%= f.password_field :password %>
    </div>
    <%= f.submit "Update Password", :class => 'btn' %>
  <% end %>
</div>

</div>
